package com.zw._35;

public class Solution {
    public int searchInsert(int[] nums, int target) {
        for(int i=0;i<nums.length;i++){
        	if
        }
    }
    public int bianrySearch(int[] nums,int start,int end,int target){
    	if(nums[(start+end)/2] == target) return (start+end)/2;
    	if(nums[(start+end)/2] > target) return bianrySearch(nums, start, (start+end)/2-1, target);
    	if(nums[(start+end)/2] < target) return bianrySearch(nums, (start+end)/2+1, end, target);
    }
}